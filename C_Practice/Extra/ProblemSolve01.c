// Q1: What is the output of this code?
#include <stdio.h>
int main() {
    int n, m;
    printf("%d", scanf("%d %d",&n,&m)); // Here the printf function counts how many 
    // integers are given by the user. The nunber of the inputs will be the answer.
    return 0;
}
// Here we gave two inputs so the output will be 2

