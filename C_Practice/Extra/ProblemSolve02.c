// Q2: What is the output of this code?
#include <stdio.h>
int main() {
    int n=20, m=2;
    printf("%d", printf("%d%d",n,m)); // Here the printf function counts how many 
    // digits are there in the given number or string
    return 0;
}
// Here we gave 20 and 2 which together has 3 digits 
// so the output will be 2023